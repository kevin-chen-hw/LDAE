# LDAE(Light Data Access Engine)
## Features
### Support partition capability by append partition field condition into CRUD SQLs
### Support the capability of injecting entity metadata during runtime 
